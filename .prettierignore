dist/
dist-types/
node_modules/
pnpm-lock.yaml

# 設計文書は手で整えている。表の桁揃えや改行位置に意味があるので触らせない。
# **正は Markdown** であり、整形で差分が出ると変更の履歴が読めなくなる。
docs/
*.md

# 試行ツールは使い捨てで、整形の対象にしない
tools/

# drizzle-kit の生成物。整形すると生成のたびに差分が出る
supabase/migrations/meta/
