// API の型定義。web と api がここだけを共有する（ADR-003）。
//
// ここに置いてよいのは **HTTP でやりとりする DTO** だけである。
// ドメインのエンティティ・値オブジェクト・ユースケースの型を持ち込まないこと。
// 持ち込むと web がドメインに依存し、ADR-003 が守ろうとしている境界が消える。
//
// 用語は `docs/domain-model.md` 第3章のユビキタス言語に合わせる。
// 禁止語（レシピ / メニュー / 候補 / 料理 / ストック / アイテム / フード）を使わない。

export {};
